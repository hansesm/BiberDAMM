

@model IEnumerable<BiberDAMM.Models.Room>
    
@{ViewBag.Title = "Raumübersicht";}

<h2>Raumübersicht</h2>

<p>
    @Html.ActionLink("Raum hinzufügen", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoomNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoomType.Name)
        </th>          
        <th>Aktionen</th>
    </tr>
 @foreach (var item in Model) {
    <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNumber)
            </td>
            <td>
                @Html.DisplayFor(modeItem => item.RoomType.Name)
            </td>           
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
                <button onclick="location.href='@Url.Action("Edit", new { id=item.Id })';return false;" class="btn btn-default" ><i class="fa fa-pencil" aria-hidden="true"></i></button>
                <button onclick="location.href='@Url.Action("Details", new { id=item.Id })';return false;" class="btn btn-default"><i class="fa fa-info" aria-hidden="true"></i></button>
                <button onclick="location.href='@Url.Action("Delete", new { id=item.Id })';return false;" class="btn btn-default"><i class="fa fa-trash-o" aria-hidden="true"></i></button>

            </td>
        </tr>
            }
        </table>