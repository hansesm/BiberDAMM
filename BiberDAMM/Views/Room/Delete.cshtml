@* AUTHOR     : [JEL] *@

@* class to delete rooms *@
@using BiberDAMM.Helpers
@model BiberDAMM.Models.Room

@{
    ViewBag.Title = "Raum löschen";
}

<h2>Raum löschen</h2>

<h3>Sind Sie sicher, dass sie dies tun möchten?</h3>
<div>
    <hr/>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RoomNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RoomNumber)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.RoomType.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RoomType.Name)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.Id)

        <div class="form-actions no-color">
            <input type="submit" name="command" value="Löschen" class="btn btn-outline-primary btn-sm"/>
            <input title="Zur Detailssicht" type="submit" name="command" value=@ConstVariables.AbortButton class="btn btn-outline-primary btn-sm" formnovalidate="formnovalidate" />
        </div>
        
    }
</div>